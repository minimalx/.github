#!/usr/bin/env bash
set -euo pipefail

# Read the release_url from the payload step’s GITHUB_OUTPUT file (already appended there)
# Nothing else to do here; the composite 'outputs' section in action.yml maps it.
# This script is a placeholder in case you want to add more computed outputs later.
:
